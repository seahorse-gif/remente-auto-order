services:
  - type: web
    name: remente-auto-order
    env: python
    buildCommand: pip install -r requirements.txt && playwright install chromium
    startCommand: python app.py
    envVars:
      - key: ORDER_SITE_URL
        value: https://ec-luft.com/aec/user/
      - key: ORDER_
